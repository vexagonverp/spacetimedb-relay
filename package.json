{
  "name": "spacetimedb-relay",
  "version": "0.0.1",
  "type": "module",
  "description": "Push-based message queue for SpacetimeDB — relay messages to consumer webhooks via procedures",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "spacetimedb": "^2.0.0"
  },
  "devDependencies": {
    "spacetimedb": "^2.0.4",
    "typescript": "~5.6.2"
  }
}
